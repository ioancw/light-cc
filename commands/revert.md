---
description: Revert file changes to a previous checkpoint
argument-hint: "[turn number or 'last']"
---

Revert file changes made during this session.

If the user specified a turn number, revert all changes from that turn onward.
Otherwise, revert the most recent set of file changes.

Use the checkpoint system to restore files to their pre-edit state. Report which files were reverted.
